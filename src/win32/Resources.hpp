#pragma once
// ---------------------------------------------------------------------------
// win32/Resources.hpp
// Numeric ids for embedded Win32 resources. Keep these in sync with app.rc.in.
// ---------------------------------------------------------------------------
#define IDI_APPICON  101   // multi-resolution RetroWall app/tray icon (app.ico)
#define IDR_LOGO_PNG 201   // 64px transparent RW logo (PNG, RCDATA) for the UI
